{
    "name": "nft-ticketing-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for NFT Ticketing Platform",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.ts"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0",
        "typescript": "^5.0.0",
        "ts-node": "^10.0.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.0.0"
    }
}